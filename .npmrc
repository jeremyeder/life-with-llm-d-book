# NPM Configuration for llm-d Book Project

# Audit configuration
# Only fail on moderate+ vulnerabilities (excludes low-severity issues)
audit-level=moderate

# Suppress funding messages during install
fund=false

# Note: Deprecated package warnings for inflight, rimraf, and glob are expected
# These are transitive dependencies from Docusaurus → webpack-dev-server
# and will be resolved when the upstream packages are updated